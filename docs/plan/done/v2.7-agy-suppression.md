# Plan: v2.7 — agy/Gemini suppression pack + closure re-anchor in the core

**Status: done — executed 2026-08-23, same day; release pending.** Ordered by the owner 2026-08-23 after reading the aiobox agy transcript (`~/.gemini/antigravity-cli/brain/c647f495-…/transcript.jsonl`). Method: 2 rounds of `/akithink` (wide, then deep), recorded below per `think.B1–B3`.

## 1. Incidents this plan answers

| Incident | Harness | Failure class |
|---|---|---|
| 2026-08-23 aiobox: prompt opened with "cho /akiflow nạp hết mọi /akirule…"; agy (Gemini 3.7 Flash High) read 20 files, never opened akiflow's SKILL.md, and began solo implementation until scolded twice; mid-scolding it copied an icon into a gitignored build-artifact dir it had just read the SSoT comment for, then deleted it | agy | **Routing**: skill token mid-sentence is model-voluntary on agy; no dispatch rule existed in the hard-loaded surface. Plus panic side-actions during COMMUNICATION |
| Owner-reported (earlier, aiobox): Claude ran /akiflow correctly but at final acceptance never re-read the original prompt, missing an explicit "check lại prompt ban đầu" demand | Claude Code | **Closure**: no always-loaded rule requires re-reading the originating request verbatim at completion; akiflow's gate checks REQ coverage only inside akiflow |
| Standing owner report: agy acts during COMMUNICATION, compresses/short-cuts prompts, "lanh chanh" — model-independent (also on Opus via agy), so the cause is Antigravity's own system prompt; the only lever is the hard-loaded `~/.gemini/GEMINI.md` | agy | **Bias**: helpfulness / shortcut / eagerness biases override rules 8–9 in long interactive sessions |

## 2. Decision record (akithink, 2 rounds)

**Goal chain**: these 3 edits → agents stop dropping prompt parts and stop acting during communication → owner's prompts execute with fidelity on every harness without repeated corrections (ultimate: trust the tooling, zero wasted interrupts).

**Facts**: GEMINI.md rules 0–13 cover scope and communication but contain no routing/dispatch rule and no closure re-anchor. agy has native skill discovery (`~/.gemini/config/skills.json` → `agskills/`), so the SKILL.md files are present on disk; only the *decision to read them* fails. The deployed GEMINI.md carries a duplicate `## 9` heading, injected by `install.py:846`.

**Known limit (assumption, flagged)**: `docs/research/gemini-helpfulness-bias-enforcement-2.md` showed the 6-trap suite cannot measure GEMINI.md efficacy (null arm scored 6/6), so the intensity additions here are **unmeasured by instrument**; the owner ordered them knowingly. Reopen trigger: a multi-turn fixture whose null arm actually fails.

**Critique highlights (round 2)**:
- *Closure re-anchor in core* — attacked as redundant with `agent.A4`/`B2` and akiflow's gate; survives because B2 governs claim honesty, not re-reading the request, and the gate exists only inside akiflow. Two incidents across two harnesses + explicit owner order clear the evidence bar. Cost: one bullet.
- *Text inflation risk in GEMINI.md* — longer file dilutes salience per token. Accepted: the file's own documented design is deliberate repetition; owner chose intensity over brevity for this surface specifically.
- *Insert vs append for the dispatch rule* — inserting at the top would maximize salience but renumber every rule and break the file's internal cross-references; **append as rule 14**, with a CHECK 0 hook inside rule 12's mandatory checklist so it still fires pre-action.

**Rejected alternatives**: put the re-anchor only in akiflow/akiship skills (opt-in surfaces, loses generic coverage); move the agy suppression text into shared `RULE-agent-behavior.md` (bloats every harness — violates the owner's constraint 3); renumber GEMINI.md to make dispatch rule 1 (breaks references for no measured gain).

## 3. Changes

1. **`payload/RULE-agent-behavior.md`** — add one bullet to `agent.B2`: closure re-anchor (re-read the originating request verbatim before reporting a multi-step task complete; tick off every explicit demand; report misses, never absorb them). Cross-harness, minimal chars.
2. **`payload/GEMINI.md`** —
   - preamble repetition note extended to name the communication read-only restatements as deliberate;
   - rule 8: SUSPENDED BIASES block (helpfulness, shortcut/summarize, eagerness-to-act) — acting during COMMUNICATION named as the most-punished failure;
   - rule 9: retitled with the literal keyword **NO YAPPING AT ALL** plus one enforcement bullet;
   - rule 12: new `CHECK 0` — undispatched `/skill` token check before any tool call;
   - new **rule 14 — Skill-token dispatch, process before product**: a `/token` naming an installed skill MUST be dispatched (read SKILL.md, follow protocol) before any other action, even mid-sentence; bundled prompts run the process directive first; includes the closure re-anchor duty mirrored for agy.
3. **`install.py`** — the appended "Shared rule source" block renumbered `## 9` → `## 15` (collision with rule 9, now also following rule 14).
4. **`CHANGELOG.md`** — `[Unreleased]` entries for all of the above.

## 4. Verify

- `python3 skills/akiflow/scripts/scythe.py` clean on edited `.md` files (no `[WRAP]`/`[YAP]`).
- `bash install.sh`, then confirm: `~/.gemini/GEMINI.md` contains rule 14, exactly one `## 9`, one `## 15`; `~/.aki/akidevrule/RULE-agent-behavior.md` carries the B2 bullet.
- Live behavioral efficacy on agy: **unverified by construction** (see §2 known limit) — not claimed.
